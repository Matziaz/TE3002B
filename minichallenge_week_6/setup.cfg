# Installation locations for ROS 2 Python executables.
# ament uses these paths so `ros2 run` and `ros2 launch` can find the nodes.
[develop]
script_dir=$base/lib/minichallenge_week_6
[install]
install_scripts=$base/lib/minichallenge_week_6
